# PostgreSQL Connection URL
# Replace YOUR_PASSWORD with your actual database password
DATABASE_URL=postgresql://zyvox_user:YOUR_PASSWORD@ctchxjkbak4j.shares.zrok.io:PORT/zyvox

# Server Configuration
PORT=5000

# n8n Webhook URL (used as proxy endpoint to avoid CORS preflight limitations)
N8N_WEBHOOK_URL=https://YOUR_N8N_SUBDOMAIN.shares.zrok.io/webhook/YOUR_FLOW_UUID
